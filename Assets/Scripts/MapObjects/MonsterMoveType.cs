/*
 * Monster Move Type, used to check if monsters can pass certain tiles, and the cost of doing so
 * I.e, flying monsters can pass tiles that walking monsters can not.
 * */


public enum MonsterMoveType
{
    Walking,
    Flying,
}